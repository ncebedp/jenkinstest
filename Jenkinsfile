pipeline {
    agent any

    stages {
        stage('Test') {
            steps {
                sh 'echo Running tests...'
                // Yahan apna test command likho, for example:
                // sh 'npm test'
                // sh './vendor/bin/phpunit'
                // sh 'pytest'
            }
        }
    }
}
